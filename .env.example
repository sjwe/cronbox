# Required: JWT signing secret (generate with: openssl rand -hex 32)
JWT_SECRET=

# Optional: Discord webhook for job failure alerts
DISCORD_WEBHOOK_URL=

# Optional: Base URL for links in Discord notifications
WEB_BASE_URL=http://localhost:8000
